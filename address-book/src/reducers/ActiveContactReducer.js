import {  } from '../constants/action_types';

const initialState = 0;

export default (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}
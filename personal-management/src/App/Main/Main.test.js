import React from 'react';
import Main from './Main';

describe('Main', () => {
  let wrapper;

  beforeEach(() => {
    wrapper = shallow(<Main />);
  });


  // it('has text \'Main:\'', () => {
  //   expect(wrapper.find('.main-container').text()).to.equal('Main:')
  // });



});